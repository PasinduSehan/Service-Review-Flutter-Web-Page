import 'package:flutter/material.dart';

class NearbyPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Nearby")),
      body: Center(child: Text("Nearby Page Content")),
    );
  }
}
