import 'package:flutter/material.dart';

class StartProjectPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Start new Project")),
      body: Center(child: Text("Add New project Page Content")),
    );
  }
}
