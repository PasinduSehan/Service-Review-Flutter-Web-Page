import 'package:flutter/material.dart';

class AdvertiseOnReviewApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Advertise on Review app")),
      body: Center(child: Text("Advertise on Review app Page Content")),
    );
  }
}
