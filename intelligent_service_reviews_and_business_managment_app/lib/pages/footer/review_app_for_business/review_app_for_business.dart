import 'package:flutter/material.dart';

class ReviewAppForBusiness extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Review app for Business")),
      body: Center(child: Text("Review app for Business Page Content")),
    );
  }
}
