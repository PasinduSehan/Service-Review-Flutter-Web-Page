import 'package:flutter/material.dart';

class ReviewAppDataForB2C extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Review app Data for B2C")),
      body: Center(child: Text("Review app Data for B2C Page Content")),
    );
  }
}
