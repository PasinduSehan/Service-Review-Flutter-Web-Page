import 'package:flutter/material.dart';

class ReviewAppForRestaurantOwners extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Review app for Restaurant Owners")),
      body: Center(child: Text("Review app for Restaurant Owners Page Content")),
    );
  }
}
