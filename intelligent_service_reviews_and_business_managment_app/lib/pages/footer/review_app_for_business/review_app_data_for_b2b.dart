import 'package:flutter/material.dart';

class ReviewAppDataForB2B extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Review app Data for B2B")),
      body: Center(child: Text("Review app Data for B2B Page Content")),
    );
  }
}
