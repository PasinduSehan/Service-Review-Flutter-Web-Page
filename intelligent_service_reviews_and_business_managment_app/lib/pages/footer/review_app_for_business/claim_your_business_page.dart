import 'package:flutter/material.dart';

class ClaimYourBusiness extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Claim Your Business Page")),
      body: Center(child: Text("Claim Your Business Page Content")),
    );
  }
}
