import 'package:flutter/material.dart';

class ReviewAppBlogForBusiness extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Review app Blog for Business")),
      body: Center(child: Text("Review app Blog for Business Page Content")),
    );
  }
}
