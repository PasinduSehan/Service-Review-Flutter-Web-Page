import 'package:flutter/material.dart';

class BusinessOwnerLogin extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Business Owner Login")),
      body: Center(child: Text("Business Owner Login Page Content")),
    );
  }
}
