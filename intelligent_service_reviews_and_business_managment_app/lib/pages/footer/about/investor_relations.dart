import 'package:flutter/material.dart';

class InvestorRelations extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Investor Relations")),
      body: Center(child: Text("Investor Relations Page Content")),
    );
  }
}
