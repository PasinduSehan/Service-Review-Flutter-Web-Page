import 'package:flutter/material.dart';

class Careers extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Careers")),
      body: Center(child: Text("Careers Page Content")),
    );
  }
}
