import 'package:flutter/material.dart';

class ContentGuidelines extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Content Guidelines")),
      body: Center(child: Text("Content Guidelines Page Content")),
    );
  }
}
