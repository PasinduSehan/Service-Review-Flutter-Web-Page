import 'package:flutter/material.dart';

class YourPrivacyChoices extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Your Privacy Choices")),
      body: Center(child: Text("Your Privacy Choices Page Content")),
    );
  }
}
