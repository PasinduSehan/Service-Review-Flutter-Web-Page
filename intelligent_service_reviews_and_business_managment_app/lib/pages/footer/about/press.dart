import 'package:flutter/material.dart';

class Press extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Press")),
      body: Center(child: Text("Press Page Content")),
    );
  }
}
