import 'package:flutter/material.dart';

class TrustAndSafety extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Trust & Safety")),
      body: Center(child: Text("Trust & Safety Page Content")),
    );
  }
}
