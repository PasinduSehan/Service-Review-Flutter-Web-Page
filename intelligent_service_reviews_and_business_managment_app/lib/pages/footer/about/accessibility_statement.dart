import 'package:flutter/material.dart';

class AccessibilityStatement extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Accessibility Statement")),
      body: Center(child: Text("Accessibility Statement Page Content")),
    );
  }
}
