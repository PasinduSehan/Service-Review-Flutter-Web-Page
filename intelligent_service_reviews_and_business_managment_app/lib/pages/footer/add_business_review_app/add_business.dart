


import 'package:flutter/material.dart';

class AddBusiness extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Add Business for Review app")),
      body: Center(child: Text("Review app for Add Business Page Content")),
    );
  }
}
