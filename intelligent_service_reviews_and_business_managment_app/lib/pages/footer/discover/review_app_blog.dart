import 'package:flutter/material.dart';

class ReviewAppBlog extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Review app Blog")),
      body: Center(child: Text("Review app Blog Page Content")),
    );
  }
}
