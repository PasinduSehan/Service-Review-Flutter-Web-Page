import 'package:flutter/material.dart';

class ProjectCostGuides extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Review app Project Cost Guides")),
      body: Center(child: Text("Review app Project Cost Guides Page Content")),
    );
  }
}
