import 'package:flutter/material.dart';

class ReviewAppMobile extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Review app Mobile")),
      body: Center(child: Text("Review app Mobile Page Content")),
    );
  }
}
