import 'package:flutter/material.dart';

class RSS extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("RSS")),
      body: Center(child: Text("RSS Page Content")),
    );
  }
}
