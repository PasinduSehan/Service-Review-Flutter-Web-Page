import 'package:flutter/material.dart';

class MoversPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Movers")),
      body: Center(child: Text("Movers Page Content")),
    );
  }
}
