import 'package:flutter/material.dart';

class AutoRepairPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Auto Repair")),
      body: Center(child: Text("Auto Repair Page Content")),
    );
  }
}
