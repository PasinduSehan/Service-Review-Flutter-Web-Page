import 'package:flutter/material.dart';

class TakeoutPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Takeout")),
      body: Center(child: Text("Takeout Page Content")),
    );
  }
}
