import 'package:flutter/material.dart';

class PlumbersPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Plumbers")),
      body: Center(child: Text("Plumbers Page Content")),
    );
  }
}
