import 'package:flutter/material.dart';

class HomeCleanersPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Home Cleaners")),
      body: Center(child: Text("Home Cleaners Page Content")),
    );
  }
}
